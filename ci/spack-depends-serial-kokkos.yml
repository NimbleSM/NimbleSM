spack:
  definitions:
    - compilers: [gcc@11.1.0]
    - packages:
        - seacas -x11 -mpi
        - kokkos@3.4.01 +openmp -hpx -cuda -pthread -serial -rocm -sycl ^hwloc~netloc
  specs:
    - matrix:
        - [$packages]
        - [$%compilers]
  config:
    install_tree: /opt/software
  view: /opt/view
spack:
  definitions:
    - compilers: [gcc@11.1.0]
    - packages:
        - trilinos@13.2.0 +openmp -mpi +exodus
  specs:
    - matrix:
        - [$packages]
        - [$%compilers]
  config:
    install_tree: /opt/software
  view: /opt/view